import {Type} from 'main.core';

export class Header
{
	constructor(options = {name: 'Header'})
	{
		this.name = options.name;
	}

	setName(name)
	{
		if (Type.isString(name))
		{
			this.name = name;
		}
	}

	getName()
	{
		return this.name;
	}
}